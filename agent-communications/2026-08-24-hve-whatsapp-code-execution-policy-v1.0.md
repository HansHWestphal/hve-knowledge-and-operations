# WhatsApp Code Execution Policy

**Date:** 2026-08-24
**Owner:** Hermes-coder

The `hanshermesagent` WhatsApp platform now has the `code_execution` toolset
enabled for future requests. The gateway was restarted successfully and the
active WhatsApp tool policy reports code execution enabled.

This restores the capability needed by approved workflows while retaining the
existing smart dangerous-command approval policy. Enabling the tool does not
automatically approve arbitrary destructive commands; those remain subject to
the profile's approval controls.
